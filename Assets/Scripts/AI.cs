using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AI : MonoBehaviour
{
    /*private float SimpleFlow(ProductionPoint productionPoint, DistributionPoint distributionPoint) // estimate flow between two nodes using one-step approximation
    {
        if (!distributionPoint.IsConnectedTo(productionPoint)) return 0;

        float fraction = 0;
        foreach (DistributionPoint dp in productionPoint.connections)
        {
            fraction += dp.demandFrequency;
        }
        fraction +=
    }


    private float PlanConnection(out ProductionPoint productionPoint, out DistributionPoint distributionPoint)
    {

    }*/

    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
